#ifndef __NETWORK_KERNEL_PRODUCER_H__
#define __NETWORK_KERNEL_PRODUCER_H__

#include <stdint.h>
#include <stddef.h>
#include <array>

namespace dg::kernel_producer{

    //2hrs
    
    // static inline constexpr size_t TRANSPORTATION_UNIT_SIZE = size_t{1} << 15;
    // using network_transportation_unit_t = std::array<char, TRANSPORTATION_UNIT_SIZE>;  


    // void init(peer_addr_t * peers, network_protocol_t * protocol){

    // }

    // inline void recv_wait(network_transportation_unit_t&) noexcept{

    // } 

    // inline auto recv_nowait(network_transportation_unit_t&) noexcept -> bool{

    // }
} 

#endif