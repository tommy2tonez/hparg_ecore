#define DEBUG_MODE_FLAG true 

#include "network_tile_member_access.h"
#include "network_tileops_host_static.h"
#include "network_tile_member_getsetter.h"

int main(){

    //I'll try to have this completed in 2 days - I think it's feasible - then we will work on the gradients
    //the gradient problem can be briefly summarized in the math_approx
    //which can be described as if-else transformers - 
    //assume that we have N GPT-3 models 
    //what we are trying to achieve is applying 0,1 transistor to the model
    //to actually get the gradient of the search is a notoriously tough problem to solve - but I'll try my best
    //
}